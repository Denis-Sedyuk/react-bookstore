export enum ROUTE {
  HOME = "/",
  SEARCH = "/search",
  BOOK = "/books/",
  CART = "cart",
  FAVORITES = "favorites",
  ACCOUNT = "account",
  USER = "user",
  RESET_PASSWORD = "reset",
  NOT_FOUND = "*",
}
