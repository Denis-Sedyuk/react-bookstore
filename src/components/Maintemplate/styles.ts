import styled from "styled-components";

export const Wrapper = styled.div`
  display: grid;
  grid-template-rows: auto 1fr auto;
  margin: 0 auto;
  text-align: center;
  min-height: 100vh;
  max-width: 1120px;
`;
