import styled from "styled-components";
import { Color } from "../../ui/colors";

export const Label = styled.p`
  font-weight: 700;
  font-size: 16px;
  line-height: 32px;
  color: ${Color.Primary};
  text-align: start;
`;
